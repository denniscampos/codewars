function isLockNessMonster(s) {
  const treeFiddy = "tree fiddy";
  const fiddyTree = 3.5;

  if (s.includes(treeFiddy) || s.includes(fiddyTree)) {
    return true;
  } else {
    return false;
  }
}
